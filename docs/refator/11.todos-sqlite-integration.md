# 11.todos-sqlite-integration

## 发现的问题

todos 路由无集成测；`toTodo`、CRUD、DELETE `affectedRows` 404 仅为注释与下层纯函数覆盖。

## 问题证据和影响范围

| 项 | 说明 |
| ---- | ------ |
| 证据 | reflection 优先；PATCH 优化前置要求 integration |
| 影响 | 新增 `src/worker/routes/todos.test.ts` |
| 生产 | 零改动 |

## 最小修改方案

真 sqlite 临时目录 + mini app（db middleware + todosRoute + error handler）；beforeEach 清空表。

Cases：POST 201 形状、GET list、GET []、PATCH 更新、DELETE 204→404、PATCH missing 404。

## Design Challenger 结论

- **verdict:** REVISE → 收窄后实施（禁 mock；限 5–7 cases）

## TDD 过程

characterization 直接锁当前行为。

## 最终修改

- `todos.test.ts` 6 cases

## Code Reviewer 结论

- **APPROVED**

## 验证结果

`pnpm test` → **60 pass**。

## 后续候选

ORDER BY + keep-unchanged past dueDate 扩展。
