# 13.pick-locale-tests

## 发现的问题

`pickLocale` 递归 leaf/fallback/array 分支复杂，零覆盖。

## 问题证据和影响范围

| 项 | 说明 |
| ---- | ------ |
| 证据 | SSG messages 全站依赖；leaf 判定与 fallback 链非显然 |
| 影响 | 新增 `src/lib/i18n-static.test.ts` |
| 生产 | 零改动 |

## 最小修改方案

leaf 命中、DEFAULT 回退、嵌套、数组、mixed keys、primitive、empty object。

## Design Challenger 结论

- **verdict:** APPROVE

## 最终修改

- 7 cases（修正 empty-object 期望为保留 `{}`，与实现一致）

## Code Reviewer 结论

- characterization 与实现一致

## 验证结果

`pnpm test` → **72 pass**。
