# 16.remove-unused-react-import-list

## 发现的问题

`list.tsx` 的 `import * as React from "react"` 未使用（新 JSX transform）。

## 问题证据和影响范围

| 项 | 说明 |
| ---- | ------ |
| 证据 | pi-lens unused；文件无 `React.` |
| 影响 | 仅 `list.tsx` |
| 行为 | 不变 |

## 最小修改方案

删除该 import。

## Design Challenger 结论

- **verdict:** APPROVE

## 最终修改

- 删 1 行 import

## 验证结果

eslint/tsc 触及文件通过。
