# 08.i18n-path-helpers-tests

## 发现的问题

`localizePath` / `stripLocalePrefix` 是路由 locale 前缀 SSOT，零覆盖。

## 问题证据和影响范围

| 项 | 说明 |
| ---- | ------ |
| 证据 | 分支含外链、已有 locale 替换、query/hash、尾 `/` |
| 影响 | 新增 `src/lib/i18n-utils.test.ts` |
| 生产 | 零改动 |

## 最小修改方案

locale 用 `en` / `zh-CN`；`replacePathLocale` 仅 1 条等价断言。

## Design Challenger 结论

- **verdict:** APPROVE（收窄后）

## TDD 过程

characterization。

## 最终修改

- `i18n-utils.test.ts`

## Code Reviewer 结论

- 批次 APPROVED（契约与实现一致）

## 验证结果

`pnpm test` 全绿。
