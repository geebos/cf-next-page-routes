# 07.get-engine-tests

## 发现的问题

`getEngine()` 是 DB 引擎 SSOT，serve/middleware 依赖；无单测。

## 问题证据和影响范围

| 项 | 说明 |
| ---- | ------ |
| 证据 | `=== "sqlite" ? "sqlite" : "d1"`；unset/other 均 d1 |
| 影响 | 扩展 `src/worker/db/engine.test.ts` |
| 生产 | 零改动 |

## 最小修改方案

beforeEach/afterEach 恢复 `DB_ENGINE`；unset→d1、sqlite→sqlite、other→d1。

## Design Challenger 结论

- **verdict:** APPROVE

## TDD 过程

characterization。

## 最终修改

- `engine.test.ts` 增加 `getEngine` suite

## Code Reviewer 结论

- 纳入测试批次 APPROVED

## 验证结果

`pnpm test` 全绿。
