# 10.get-base-url-tests

## 发现的问题

`getBaseUrl` / `getSiteUrl` 含 production fail-loud 与 URL 形状校验，零覆盖；api/SEO 依赖。

## 问题证据和影响范围

| 项 | 说明 |
|----|------|
| 证据 | 非法 base throw；prod unset throw；dev unset `""` / example.com |
| 影响 | 扩展 `src/lib/i18n-utils.test.ts` |
| 生产 | 零改动 |

## 最小修改方案

beforeEach/afterEach 恢复 env；`NODE_ENV` 用 `defineProperty` 写入。

## Design Challenger 结论

- **verdict:** APPROVE

## TDD 过程

characterization。

## 最终修改

- `getBaseUrl / getSiteUrl` suite（7 cases）

## Code Reviewer 结论

- 纳入测试批次；行为与实现一致

## 验证结果

`pnpm test` → **54 pass**。

## 后续候选

见 Ralph task 文件 Reflection 小节。
