# 14.create-app-sqlite-smoke

## 发现的问题

现有测均为 mini app，从不执行 `createApp()`；装配漂移可静默通过。

## 问题证据和影响范围

| 项 | 说明 |
| ---- | ------ |
| 证据 | createApp 注释要求 CF/Node 共用装配 |
| 影响 | 新增 `src/worker/create-app.test.ts` |
| 生产 | 零改动 |

## 最小修改方案

真 sqlite 临时库：`GET /api/health` → ok；`GET /api/todos` → `[]`。

## Design Challenger 结论

- **verdict:** APPROVE

## 最终修改

- 2 smoke cases

## Code Reviewer 结论

- 装配 smoke 合格

## 验证结果

`pnpm test` → **72 pass**。
