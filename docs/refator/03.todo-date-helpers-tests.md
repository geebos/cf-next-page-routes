# 03.todo-date-helpers-tests

## 发现的问题

`shared/schemas/todo.ts` 日期 helper（UTC midnight、loose range、picker 往返）无单测，时区契约易回归。

## 问题证据和影响范围

| 项 | 说明 |
| ---- | ------ |
| 证据 | helper 有明确非显然契约；全仓几乎仅 `db.test.ts` |
| 影响 | 仅新增 `src/shared/schemas/todo.test.ts` |
| 生产 | 零改动 |

## 最小修改方案

`node:test` 覆盖 midnight / loose range / format / picker roundtrip / localStartOfToday*（注入 now）+ PRIORITY SSOT 对齐。

## Design Challenger 结论

- **verdict:** APPROVE（独立测试轮）

## TDD 过程

纯加测；实现已存在 → 直接 green。

## 最终修改

- 新增 `todo.test.ts`（8 cases）

## Code Reviewer 结论

- **APPROVED**（与 error/validator 一并审）

## 验证结果

`pnpm test` 全绿（含本文件）。

## 后续候选

error/validator 契约测、affectedRows 测。
