# 06.affected-rows-tests

## 发现的问题

`affectedRows` 统一 D1 `meta.changes` 与 sqlite `changes`，DELETE 404 依赖它，无单测。

## 问题证据和影响范围

| 项 | 说明 |
| ---- | ------ |
| 证据 | `todos.ts` DELETE `changes === 0 → 404` |
| 影响 | 新增 `src/worker/db/engine.test.ts` |
| 生产 | 零改动 |

## 最小修改方案

5 条纯函数断言：D1 / sqlite / 缺省 0 / 显式 0 / meta 优先。

## Design Challenger 结论

- **verdict:** APPROVE

## TDD 过程

characterization。

## 最终修改

- `engine.test.ts`

## Code Reviewer 结论

- **APPROVED**（并入批量测试审查）

## 验证结果

`pnpm test` 全绿。

## 后续候选

i18n path helpers 测试（需 zh-CN）；TodoPage 拆分评估；PATCH 双 select。
