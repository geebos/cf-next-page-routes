# 05.json-validator-characterization

## 发现的问题

`jsonValidator` 是 todos POST/PATCH 入参闸门，无单测。

## 问题证据和影响范围

| 项 | 说明 |
| ---- | ------ |
| 证据 | 失败抛 BusinessError → error handler 统一 body |
| 影响 | 新增 `src/worker/lib/validator.test.ts` |
| 生产 | 零改动 |

## 最小修改方案

合法 JSON → 200 + parsed；非法 → 400 + 首条 issue message。

## Design Challenger 结论

- **verdict:** APPROVE（可与 error 同主题；本轮单独记录）

## TDD 过程

characterization。

## 最终修改

- `validator.test.ts` 2 cases
- 捎带 `package.json` `"test": "tsx --test \"src/**/*.test.ts\""`（非独立轮）

## Code Reviewer 结论

- **APPROVED**

## 验证结果

`pnpm test` → 27 pass。

## 后续候选

affectedRows 单测。
