# 17.export-isActive-nav-tests

## 发现的问题

`isActive` 含 locale strip + 尾斜杠规范化，未 export、无测。

## 问题证据和影响范围

| 项 | 说明 |
|----|------|
| 证据 | Sidebar/Tabbar 活跃态依赖此比较 |
| 影响 | `navigate.tsx` export；新增 `navigate.test.ts` |

## 最小修改方案

export 同文件函数 + 表驱动单测；不抽 lib。

## Design Challenger 结论

- **verdict:** APPROVE（仅 export；抽 lib REJECT）

## 最终修改

- `export function isActive`
- 5 cases

## 验证结果

`pnpm test` 全绿。
