# 09.settings-locale-tests

## 发现的问题

`isSupportedLocale` / `normalizeLocale` 有语言前缀映射（`zh`→`zh-CN`），无单测。

## 问题证据和影响范围

| 项 | 说明 |
| ---- | ------ |
| 证据 | `normalizeLocale` 分支：精确匹配 / 语言前缀 / default |
| 影响 | 新增 `src/i18n/settings.test.ts` |
| 生产 | 零改动 |

## 最小修改方案

en/zh-CN 接受；`zh`/`zh-TW`→zh-CN；null/unknown→DEFAULT_LOCALE。

## Design Challenger 结论

- **verdict:** APPROVE

## TDD 过程

characterization。

## 最终修改

- `settings.test.ts`

## Code Reviewer 结论

- 批次 APPROVED

## 验证结果

`pnpm test` 全绿。
